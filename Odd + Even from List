# To write a program to accept a list from the user and display
# the sum of odd and even elements.

list = []
sum1 = 0
sum2 = 0

n = int(input("Enter number of Elements : "))

for i in range(1, n+1):
value = int(input("Enter value of element : "))
list.append(value)

for j in range(n):
if list[j] % 2 == 0:
sum1 = sum1 + list[j]
else:
sum2 = sum2 + list[j]

print("Sum of even numbers = ", sum1)
print("Sum of odd numbers = ", sum2)
